[package]
name = "cubemap"
version = "0.1.0"
edition = "2024"

[dependencies]
rswebgl = { workspace = true }
glam = { workspace = true }
wasm-bindgen = { workspace = true }
web-sys = { workspace = true, features = [
    "console",
    "Document",
    "Element",
    "HtmlCanvasElement",
    "Window",
] }
